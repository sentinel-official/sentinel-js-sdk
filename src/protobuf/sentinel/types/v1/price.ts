// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v4.25.9
// source: sentinel/types/v1/price.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "sentinel.types.v1";

export interface Price {
  denom: string;
  baseValue: string;
  quoteValue: string;
}

function createBasePrice(): Price {
  return { denom: "", baseValue: "", quoteValue: "" };
}

export const Price: MessageFns<Price> = {
  encode(message: Price, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.baseValue !== "") {
      writer.uint32(18).string(message.baseValue);
    }
    if (message.quoteValue !== "") {
      writer.uint32(26).string(message.quoteValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Price {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baseValue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quoteValue = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Price {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      baseValue: isSet(object.baseValue)
        ? globalThis.String(object.baseValue)
        : isSet(object.base_value)
        ? globalThis.String(object.base_value)
        : "",
      quoteValue: isSet(object.quoteValue)
        ? globalThis.String(object.quoteValue)
        : isSet(object.quote_value)
        ? globalThis.String(object.quote_value)
        : "",
    };
  },

  toJSON(message: Price): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.baseValue !== "") {
      obj.baseValue = message.baseValue;
    }
    if (message.quoteValue !== "") {
      obj.quoteValue = message.quoteValue;
    }
    return obj;
  },

  create(base?: DeepPartial<Price>): Price {
    return Price.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Price>): Price {
    const message = createBasePrice();
    message.denom = object.denom ?? "";
    message.baseValue = object.baseValue ?? "";
    message.quoteValue = object.quoteValue ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
